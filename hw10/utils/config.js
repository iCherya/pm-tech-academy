const GITHUB_API = {
    base: 'https://api.github.com',
    searchUsers: 'search/users',
    searchUser: 'users'
};

const LIVE_SEARCH = {
    maxResults: 5,
    debounceTime: 1000
};

export { GITHUB_API, LIVE_SEARCH };
